#Python script to remove the last digit from a given number.
number = int(input("Enter a number : "))

remove_last_digit = number/10

print("Remove the last digit of %d, now number is %d" %(number,remove_last_digit))
